Place brand assets in this folder.

Expected runtime logo path:

- `apps/ui/public/brand/lobester-logo.png`

The dashboard header will auto-load that file when present and fall back to a styled "LS" badge when missing.
